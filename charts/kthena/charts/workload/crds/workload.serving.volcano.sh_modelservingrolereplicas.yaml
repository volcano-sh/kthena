---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: modelservingrolereplicas.workload.serving.volcano.sh
spec:
  group: workload.serving.volcano.sh
  names:
    kind: ModelServingRoleReplica
    listKind: ModelServingRoleReplicaList
    plural: modelservingrolereplicas
    singular: modelservingrolereplica
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ModelServingRoleReplica is the Schema for the modelservingrolereplicas
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ModelServingRoleReplicaSpec defines the desired state of
              ModelServingRoleReplica
            properties:
              modelServingRef:
                description: ModelServingRef references the parent ModelServing
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              replicas:
                default: 1
                description: Replicas maps to this role's replica count
                format: int32
                type: integer
              roleName:
                description: RoleName specifies the target role (e.g., "prefill",
                  "decode")
                type: string
            required:
            - modelServingRef
            - replicas
            - roleName
            type: object
          status:
            description: ModelServingRoleReplicaStatus defines the observed state
              of ModelServingRoleReplica
            properties:
              labelSelector:
                description: LabelSelector is a label query over pods that should
                  match the replica count.
                type: string
              replicas:
                description: Replicas is the observed target replica count for the
                  role from the ModelServing object
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      scale:
        labelSelectorPath: .status.labelSelector
        specReplicasPath: .spec.replicas
        statusReplicasPath: .status.replicas
      status: {}
