apiVersion: v1
kind: Service
metadata:
  name: podinfo
  namespace: autoscale-demo
  labels:
    app.kubernetes.io/name: podinfo
spec:
  type: ClusterIP
  selector:
    modelserving.volcano.sh/name: podinfo-ms
    modelserving.volcano.sh/role: podinfo
  ports:
  - name: http
    port: 9898
    protocol: TCP
    targetPort: http
  - name: grpc
    port: 9999
    protocol: TCP
    targetPort: grpc
