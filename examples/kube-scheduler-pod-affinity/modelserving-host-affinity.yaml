# ModelServing scoped co-location.
#
# Topology key: kubernetes.io/hostname
# Effect: all pods of the ModelServing prefer the same node.
#
# ModelServing: modelserving-affinity
# |
# +-- ServingGroup-0
# |   +-- prefill: [entry, worker] -> node-a
# |   +-- decode:  [entry, worker] -> node-a
# |
# +-- ServingGroup-1
#     +-- prefill: [entry, worker] -> node-a
#     +-- decode:  [entry, worker] -> node-a
apiVersion: workload.serving.volcano.sh/v1alpha1
kind: ModelServing
metadata:
  name: modelserving-affinity
  namespace: default
spec:
  schedulerName: default-scheduler
  replicas: 2
  template:
    restartGracePeriodSeconds: 60
    roles:
      - name: prefill
        replicas: 1
        entryTemplate:
          spec:
            affinity:
              podAffinity:
                preferredDuringSchedulingIgnoredDuringExecution:
                  - weight: 100
                    podAffinityTerm:
                      topologyKey: kubernetes.io/hostname
                      labelSelector:
                        matchLabels:
                          modelserving.volcano.sh/name: modelserving-affinity
            containers:
              - name: leader
                image: ghcr.io/llm-d/llm-d-inference-sim:latest
                imagePullPolicy: IfNotPresent
                args:
                  - --model=kthena-pod-affinity-sim
                  - --served-model-name=kthena-pod-affinity-sim
                  - --port=8000
                  - --mode=echo
                  - --time-to-first-token=0
                  - --inter-token-latency=0
                ports:
                  - containerPort: 8000
        workerReplicas: 1
        workerTemplate:
          spec:
            affinity:
              podAffinity:
                preferredDuringSchedulingIgnoredDuringExecution:
                  - weight: 100
                    podAffinityTerm:
                      topologyKey: kubernetes.io/hostname
                      labelSelector:
                        matchLabels:
                          modelserving.volcano.sh/name: modelserving-affinity
            containers:
              - name: worker
                image: ghcr.io/llm-d/llm-d-inference-sim:latest
                imagePullPolicy: IfNotPresent
                args:
                  - --model=kthena-pod-affinity-sim
                  - --served-model-name=kthena-pod-affinity-sim
                  - --port=8000
                  - --mode=echo
                  - --time-to-first-token=0
                  - --inter-token-latency=0
                ports:
                  - containerPort: 8000
      - name: decode
        replicas: 1
        entryTemplate:
          spec:
            affinity:
              podAffinity:
                preferredDuringSchedulingIgnoredDuringExecution:
                  - weight: 100
                    podAffinityTerm:
                      topologyKey: kubernetes.io/hostname
                      labelSelector:
                        matchLabels:
                          modelserving.volcano.sh/name: modelserving-affinity
            containers:
              - name: leader
                image: ghcr.io/llm-d/llm-d-inference-sim:latest
                imagePullPolicy: IfNotPresent
                args:
                  - --model=kthena-pod-affinity-sim
                  - --served-model-name=kthena-pod-affinity-sim
                  - --port=8000
                  - --mode=echo
                  - --time-to-first-token=0
                  - --inter-token-latency=0
                ports:
                  - containerPort: 8000
        workerReplicas: 1
        workerTemplate:
          spec:
            affinity:
              podAffinity:
                preferredDuringSchedulingIgnoredDuringExecution:
                  - weight: 100
                    podAffinityTerm:
                      topologyKey: kubernetes.io/hostname
                      labelSelector:
                        matchLabels:
                          modelserving.volcano.sh/name: modelserving-affinity
            containers:
              - name: worker
                image: ghcr.io/llm-d/llm-d-inference-sim:latest
                imagePullPolicy: IfNotPresent
                args:
                  - --model=kthena-pod-affinity-sim
                  - --served-model-name=kthena-pod-affinity-sim
                  - --port=8000
                  - --mode=echo
                  - --time-to-first-token=0
                  - --inter-token-latency=0
                ports:
                  - containerPort: 8000
