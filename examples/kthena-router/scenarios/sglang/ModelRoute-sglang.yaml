apiVersion: networking.serving.volcano.sh/v1alpha1
kind: ModelRoute
metadata:
  name: sglang
  namespace: default
spec:
  modelName: sglang
  rules:
    - name: default
      targetModels:
        - modelServerName: sglang-mock
