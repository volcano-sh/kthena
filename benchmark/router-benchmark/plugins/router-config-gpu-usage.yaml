# Router scheduler config — GPU Usage
apiVersion: v1
kind: ConfigMap
metadata:
  name: kthena-router-config
  namespace: kthena-system
data:
  routerConfiguration: |
    scheduler:
      plugins:
        Filter:
          enabled: []
          disabled:
            - least-request
            - lora-affinity
        Score:
          enabled:
            - name: gpu-usage
              weight: 1
